// NOTE: DAO interface retained for reference only. The app now uses JSON file storage
// via ContactRepository; this DAO is not used anymore.

package com.cdhgold.clickcall.data

// Placeholder interface kept for reference; implementation removed.
interface ContactDao {
    // Previously contained Room DAO methods. Now unused.
}
